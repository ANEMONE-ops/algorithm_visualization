"""
算法可视化系统 - 知识库模块
"""

from fastapi import APIRouter, HTTPException

router = APIRouter(prefix="/api/knowledge", tags=["知识库"])

# ============================================================
# 知识库内容
# ============================================================

KNOWLEDGE_BASE = {
    "bubble_sort": {
        "title": "冒泡排序 (Bubble Sort)",
        "category": "排序算法",
        "overview": """
冒泡排序是最简单的排序算法之一。它重复地遍历要排序的数组，一次比较两个相邻元素，如果它们的顺序错误就把它们交换过来。遍历数组的工作会重复进行，直到没有需要交换的元素为止。

### 算法思想
就像水里的气泡从底部升到水面一样，较小的元素会慢慢"浮"到数组的顶端。

### 算法步骤
1. 比较相邻的两个元素。如果第一个比第二个大，就交换它们。
2. 对每一对相邻元素做同样的工作，从开始第一对到结尾的最后一对。这一步完成后，最后的元素会是最大的数。
3. 针对所有元素重复以上步骤，除了最后一个。
4. 持续每次对越来越少的元素重复上述步骤，直到没有任何一对数字需要比较。

### 优化
如果某一轮遍历中没有发生任何交换，说明数组已经有序，可以提前结束排序。
""",
        "how_to_use": """
### 在本系统中的使用
1. 在左侧算法列表中选择"冒泡排序"
2. 输入数组数据（逗号分隔），例如：64, 34, 25, 12, 22, 11, 90
3. 点击"开始运行"
4. 观察可视化画布中的柱状图变化
5. 橙色高亮的柱子表示正在比较/交换的元素
6. 绿色柱子表示已经排好序的元素

### 观察要点
- 每一轮遍历会将剩余元素中的最大值"冒泡"到最后
- 当数组近乎有序时，算法会提前结束
- 注意比较次数和交换次数的关系
""",
        "examples": [
            {
                "title": "例题1：基本排序",
                "question": "使用冒泡排序对数组 [29, 10, 14, 37, 13] 进行升序排序，写出每一轮结束后的数组状态。",
                "steps": [
                    "初始: [29, 10, 14, 37, 13]",
                    "第1轮: 比较 29>10 交换 → [10,29,14,37,13], 29>14 交换 → [10,14,29,37,13], 29<37 不换, 37>13 交换 → [10,14,29,13,37]。第1轮结束，37已就位。",
                    "第2轮: 10<14 不换, 14<29 不换, 29>13 交换 → [10,14,13,29,37]。第2轮结束，29已就位。",
                    "第3轮: 10<14 不换, 14>13 交换 → [10,13,14,29,37]。第3轮结束，14已就位。",
                    "第4轮: 10<13 不换，本轮无交换，提前结束！"
                ],
                "answer": "经过4轮遍历（实际第4轮检测到有序提前退出），最终结果：[10, 13, 14, 29, 37]"
            },
            {
                "title": "例题2：交换次数计算",
                "question": "对数组 [5, 1, 4, 2, 8] 进行冒泡排序，总共发生多少次交换？",
                "steps": [
                    "第1轮: 5>1换(1), 5>4换(2), 5>2换(3), 5<8不换 → [1,4,2,5,8]，3次交换",
                    "第2轮: 1<4不换, 4>2换(4), 4<5不换 → [1,2,4,5,8]，1次交换",
                    "第3轮: 1<2不换, 2<4不换 → 本轮无交换，结束！"
                ],
                "answer": "总共发生4次交换，最终结果：[1, 2, 4, 5, 8]"
            },
            {
                "title": "例题3：最优/最坏情况",
                "question": "分析冒泡排序在以下两种情况下的比较次数：(1) 数组[1,2,3,4,5]  (2) 数组[5,4,3,2,1]",
                "steps": [
                    "(1) 已有序: 第1轮遍历比较4次，无交换，提前结束。比较次数=4",
                    "(2) 完全逆序: 第1轮4次比较+3次交换，第2轮3次比较+2次交换，第3轮2次比较+1次交换，第4轮1次比较+0次交换。比较次数=4+3+2+1=10",
                    "最好情况 O(n)，最坏情况 O(n²)"
                ],
                "answer": "最好情况比较4次（n-1），最坏情况比较10次（n(n-1)/2）"
            }
        ]
    },

    "quick_sort": {
        "title": "快速排序 (Quick Sort)",
        "category": "排序算法",
        "overview": """
快速排序是由Tony Hoare于1960年提出的一种高效的排序算法。它采用分治（Divide and Conquer）策略，将一个数组分成两个子数组，然后递归地对子数组进行排序。

### 算法思想
1. 从数组中选一个"基准"（pivot）元素
2. 将数组分为两部分：左边所有元素 ≤ 基准，右边所有元素 > 基准
3. 递归地对左右两部分进行快速排序

### 分区方案
- **Lomuto方案**：选择最后一个元素作为基准，维护一个指针i表示小于等于基准的边界
- **Hoare方案**：使用双指针从两端向中间移动

### 性能特点
- 平均时间复杂度：O(n log n)
- 最好情况：每次基准都能将数组平分，O(n log n)
- 最坏情况：每次基准都是最大/最小值，退化为O(n²)
""",
        "how_to_use": """
### 在本系统中的使用
1. 选择"快速排序"算法
2. 输入数组数据或点击测试用例
3. 观察每次分区过程：基准元素以特殊颜色标识
4. 递归调用以"📥 递归进入"和"📤 递归返回"标注

### 观察要点
- 基准的选择方式和位置
- 分区后左侧 ≤ 基准，右侧 > 基准
- 递归深度反映了分治的层数
        """,
        "examples": [
            {
                "title": "例题1：分区过程",
                "question": "对数组 [8, 3, 9, 5, 2, 7] 使用快速排序（选最后元素为基准），画出第一次分区后的数组状态。",
                "steps": [
                    "基准 pivot = 7（最后一个元素）",
                    "遍历: 8>7 不处理, 3≤7 i=0交换arr[0]↔arr[1] → [3,8,9,5,2,7]",
                    "9>7 不处理, 5≤7 i=2交换arr[2]↔arr[3] → [3,5,9,8,2,7]",
                    "2≤7 i=3交换arr[3]↔arr[4] → [3,5,2,8,9,7]",
                    "最后将基准放到i+1=4位置: [3,5,2,7,9,8]"
                ],
                "answer": "第一次分区后：[3, 5, 2, 7, 9, 8]，基准7已在正确位置（索引3）"
            },
            {
                "title": "例题2：时间复杂度",
                "question": "当数组已经有序时，快速排序的时间复杂度是多少？为什么？如何避免？",
                "steps": [
                    "已有序数组（如递增）：每次选最后一个元素为基准，它是最大值",
                    "分区后：左边n-1个元素，右边0个元素 → 递归深度为n",
                    "每层比较n次 → 总比较次数≈n+(n-1)+...+1 = n(n-1)/2 = O(n²)",
                    "避免方法：随机选择基准、三数取中法选择基准"
                ],
                "answer": "已有序时退化为O(n²)。可通过随机选取基准或三数取中法优化"
            }
        ]
    },

    "mst": {
        "title": "Prim最小生成树",
        "category": "图算法",
        "overview": """
最小生成树（Minimum Spanning Tree, MST）是一个连通加权无向图中一棵权值最小的生成树。生成树包含图中的所有顶点，且边的数量恰好为V-1条（V为顶点数）。

### Prim算法思想
从任意一个顶点开始，每次选择连接"已访问顶点集合"和"未访问顶点集合"的权重最小的边，将该边加入MST，并将新顶点标记为已访问。重复直到所有顶点都被包含。

### 算法步骤
1. 初始化：选择任意起始顶点，标记为已访问
2. 循环V-1次：
   - 扫描所有连接已访问和未访问顶点的边
   - 选择权重最小的边
   - 将该边加入MST，新顶点标记为已访问
3. 得到包含V-1条边的最小生成树

### 与Kruskal算法的区别
- Prim：基于顶点扩展，适合稠密图 O(V²)
- Kruskal：基于边排序+并查集，适合稀疏图 O(E log E)
""",
        "how_to_use": """
### 在本系统中的使用
1. 选择"最小生成树(Prim算法)"
2. 输入顶点数和邻接矩阵（0表示无边）
3. 观察图中的节点颜色变化：蓝色=未访问，绿色=已访问，橙色=当前选中
4. 实线绿边=MST边，虚线橙边=候选边
5. 每条边上的数字是权重
        """,
        "examples": [
            {
                "title": "例题1：手算Prim",
                "question": "给定4个顶点的完全图，边权分别为：AB=2, AC=3, AD=1, BC=4, BD=5, CD=6。从A开始，用Prim算法求MST。",
                "steps": [
                    "初始: visited={A}, candidates={AB(2), AC(3), AD(1)}",
                    "选AD(1): visited={A,D}, MST={AD}, 新增candidates: DB(5), DC(6)",
                    "候选边: AB(2), AC(3), DB(5), DC(6), 选最小AB(2)",
                    "选AB(2): visited={A,D,B}, MST={AD, AB}, 新增candidates: BC(4)",
                    "候选边: AC(3), DB(5), DC(6), BC(4), 选最小AC(3)",
                    "选AC(3): visited={A,D,B,C}, MST={AD, AB, AC}"
                ],
                "answer": "MST边集：{AD(1), AB(2), AC(3)}，总权重=6"
            },
            {
                "title": "例题2：应用场景",
                "question": "某城市要在5个小区之间铺设光纤，各小区间的铺设成本（万元）如矩阵所示。求最低成本的铺设方案。",
                "steps": [
                    "这是一个典型的最小生成树应用问题",
                    "小区 = 顶点，光纤成本 = 边权重",
                    "求MST即求连接所有小区的最低总成本",
                    "使用Prim或Kruskal算法均可求解"
                ],
                "answer": "Prim算法可以高效解决此类网络设计问题，保证总成本最小。"
            }
        ]
    },

    "huffman": {
        "title": "哈夫曼树 (Huffman Tree)",
        "category": "树结构",
        "overview": """
哈夫曼树，又称最优二叉树，是一种带权路径长度最短的二叉树。由David Huffman于1952年提出，广泛应用于数据压缩领域。

### 核心概念
- **带权路径长度(WPL)**：从根节点到每个叶子节点的路径长度乘以该叶子节点权重的总和
- **哈夫曼编码**：基于哈夫曼树的前缀编码，高频字符用短编码，低频字符用长编码

### 构建步骤
1. 将每个字符及其频率作为一个独立的节点，放入最小堆
2. 从堆中取出两个频率最小的节点
3. 创建一个新节点，频率为两者之和，左右子节点分别为取出的两个节点
4. 将新节点放回堆中
5. 重复步骤2-4，直到堆中只剩下一个节点，即为哈夫曼树的根

### 特点
- 是一种贪心算法
- 生成的编码是前缀码（任何编码都不是另一个编码的前缀）
- 对于给定的频率分布，哈夫曼编码是最优的
""",
        "how_to_use": """
### 在本系统中的使用
1. 选择"哈夫曼树"
2. 输入字符和对应频率，如字符：A,B,C,D,E,F  频率：5,9,12,13,16,45
3. 观察树从底部到顶部的构建过程
4. 橙色节点表示当前正在合并的节点
5. 最终生成的编码表在结果面板显示
        """,
        "examples": [
            {
                "title": "例题1：手建哈夫曼树",
                "question": "字符A(5), B(9), C(12), D(13), E(16), F(45)，构建哈夫曼树并给出编码。",
                "steps": [
                    "初始节点: A:5, B:9, C:12, D:13, E:16, F:45",
                    "合并A+B→(AB):14, 堆: C:12, D:13, (AB):14, E:16, F:45",
                    "合并C+D→(CD):25, 堆: (AB):14, E:16, (CD):25, F:45",
                    "合并(AB)+E→(ABE):30, 堆: (CD):25, (ABE):30, F:45",
                    "合并(CD)+(ABE)→(CDABE):55, 堆: F:45, (CDABE):55",
                    "合并F+(CDABE)→Root:100",
                    "编码: F=0, C=100, D=101, A=1100, B=1101, E=111"
                ],
                "answer": "WPL = 45×1 + 12×3 + 13×3 + 5×4 + 9×4 + 16×3 = 224"
            },
            {
                "title": "例题2：压缩率计算",
                "question": "若有一段文本包含1000个字符，各字符频率同上题。使用定长编码和哈夫曼编码分别需要多少比特？",
                "steps": [
                    "定长编码: 6个字符需3位，1000×3=3000 bits",
                    "哈夫曼编码: 45×1000/100×1 + 12×1000/100×3 + 13×1000/100×3 + 5×1000/100×4 + 9×1000/100×4 + 16×1000/100×3 = 2240 bits",
                    "压缩率 = (3000-2240)/3000 ≈ 25.3%"
                ],
                "answer": "哈夫曼编码比定长编码节省约25.3%的存储空间"
            }
        ]
    },

    "hanoi": {
        "title": "汉诺塔 (Tower of Hanoi)",
        "category": "递归",
        "overview": """
汉诺塔是一个经典的递归问题，起源于印度传说。问题描述：有三根柱子（A、B、C），其中A柱上有n个大小不等的盘子，大盘在下，小盘在上。要求将所有盘子从A柱移动到C柱，每次只能移动一个盘子，且大盘不能放在小盘上面。

### 递归公式
- T(1) = 1
- T(n) = 2T(n-1) + 1
- 通项公式：T(n) = 2ⁿ - 1

### 递归策略
1. 将n-1个盘子从源柱移到辅助柱（借助目标柱）
2. 将最大的盘子从源柱移到目标柱
3. 将n-1个盘子从辅助柱移到目标柱（借助源柱）

### 复杂度
- 时间复杂度：O(2ⁿ)
- 空间复杂度：O(n)（递归调用栈）
""",
        "how_to_use": """
### 在本系统中的使用
1. 选择"汉诺塔"
2. 输入盘子数量（2-8），盘子越多步数指数增长
3. 观察三根柱子上的盘子移动
4. 每一步都会显示从哪根柱子移动到哪根柱子
5. 递归进入/退出都有明确标注
        """,
        "examples": [
            {
                "title": "例题1：3盘汉诺塔",
                "question": "写出3个盘子从A到C的完整移动序列。",
                "steps": [
                    "递推: T(3) = 2T(2)+1 = 2×3+1 = 7步",
                    "第1步: A→C (盘1)", "第2步: A→B (盘2)",
                    "第3步: C→B (盘1)", "第4步: A→C (盘3)",
                    "第5步: B→A (盘1)", "第6步: B→C (盘2)",
                    "第7步: A→C (盘1)"
                ],
                "answer": "共7步，等于2³-1=7，达到最优解。"
            },
            {
                "title": "例题2：最少步数推导",
                "question": "证明n个盘子的汉诺塔问题最少需要2ⁿ-1步。",
                "steps": [
                    "数学归纳法：",
                    "奠基: n=1时，2¹-1=1步，将唯一的盘子直接移到目标柱，正确。",
                    "假设: n=k时需要2ᵏ-1步",
                    "递推: n=k+1时，(1)将k个盘移到辅助柱: 2ᵏ-1步 (2)将最大盘移到目标柱: 1步 (3)将k个盘从辅助移到目标: 2ᵏ-1步",
                    "总计: (2ᵏ-1)+1+(2ᵏ-1)=2×2ᵏ-1=2ᵏ⁺¹-1，得证"
                ],
                "answer": "由递推公式T(n)=2T(n-1)+1和T(1)=1，可得T(n)=2ⁿ-1"
            }
        ]
    },

    "graph_coloring": {
        "title": "图着色 (Graph Coloring)",
        "category": "回溯",
        "overview": """
图着色问题是图论中的经典NP完全问题：给定一个无向图和m种颜色，要求为每个顶点分配一种颜色，使得相邻顶点颜色不同。如果可以用m种颜色完成着色，则称图是m-可着色的。

### 回溯法求解
回溯法是一种系统地搜索问题解空间的方法。对于图着色问题：
1. 从第一个顶点开始，尝试分配每种颜色
2. 检查当前顶点是否与已着色的相邻顶点颜色冲突
3. 如果颜色合法，递归处理下一个顶点
4. 如果所有颜色都不合法，回溯到上一个顶点更换颜色

### 关键概念
- **色数(Chromatic Number)**：使图可着色的最少颜色数
- **四色定理**：任何平面图都可以用4种颜色着色
""",
        "how_to_use": """
### 在本系统中的使用
1. 选择"图着色(回溯法)"
2. 输入顶点数、颜色数和邻接矩阵
3. 观察回溯过程：绿色=着色成功，红色=冲突回溯
4. 橙色高亮表示当前正在处理的顶点
5. 最终着色方案在结果面板显示
        """,
        "examples": [
            {
                "title": "例题1：回溯过程",
                "question": "一个三角形（3顶点完全图K₃）用2种颜色可以完成着色吗？演示回溯过程。",
                "steps": [
                    "K₃: 每对顶点之间都有边相连",
                    "顶点0着色0(红) → 顶点1尝试色0，与顶点0冲突 → 顶点1着色1(蓝)",
                    "顶点2尝试色0，与顶点0冲突 → 顶点2尝试色1，与顶点1冲突",
                    "所有颜色都冲突！回溯到顶点1 → 顶点1无其他颜色可选 → 回溯到顶点0",
                    "顶点0尝试色1 → 对称情况，同样失败",
                    "结论：K₃不可2-着色，色数为3"
                ],
                "answer": "K₃无法用2种颜色着色。需要至少3种颜色（每个顶点各一种）。"
            },
            {
                "title": "例题2：二分图着色",
                "question": "证明任意二分图都可以用2种颜色着色。",
                "steps": [
                    "二分图定义：顶点集可分为两个子集U和V，所有边都在U和V之间",
                    "着色策略：U中所有顶点染颜色0，V中所有顶点染颜色1",
                    "验证：U中任意两顶点无边（同色不冲突），V中同理",
                    "任一条边(u,v)连接U和V → u色0, v色1 → 不同色，满足要求"
                ],
                "answer": "二分图可用2种颜色完成着色，只需将两个分区的顶点分别染不同颜色即可。"
            }
        ]
    }
}


# ---------------------------------------------------------------------------
# 端点
# ---------------------------------------------------------------------------

@router.get("/")
def get_knowledge_list():
    """获取知识库目录"""
    items = []
    for key, kb in KNOWLEDGE_BASE.items():
        items.append({
            "id": key,
            "title": kb["title"],
            "category": kb["category"],
            "example_count": len(kb.get("examples", [])),
        })
    return {"knowledge_items": items}


@router.get("/{algo_id}")
def get_knowledge_detail(algo_id: str):
    """获取指定算法的知识库详情"""
    if algo_id not in KNOWLEDGE_BASE:
        raise HTTPException(
            status_code=404, detail=f"知识库中未找到 '{algo_id}'"
        )
    kb = KNOWLEDGE_BASE[algo_id]
    return {
        "id": algo_id,
        "title": kb["title"],
        "category": kb["category"],
        "overview": kb["overview"],
        "how_to_use": kb["how_to_use"],
        "examples": kb["examples"],
    }
